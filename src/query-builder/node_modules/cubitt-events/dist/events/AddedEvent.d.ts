import * as Common from "cubitt-common";
import { Event } from "./Event";
import { EventType } from "./../EventType";
export declare abstract class AddedEvent extends Event {
    elementId: Common.Guid;
    elementType: string;
    elementProperties: Common.Dictionary<any>;
    constructor(sourceId: Common.Guid, version: number, type: EventType, timestamp: number, elementId: Common.Guid, elementType: string, elementProperties: Common.Dictionary<any>);
}
