"use strict";
var __extends = (this && this.__extends) || function (d, b) {
    for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p];
    function __() { this.constructor = d; }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
};
var AddCommand_1 = require("./AddCommand");
var CommandType_1 = require("./../CommandType");
/**
 * A command to add a model to a project.
 */
var AddModelCommand = (function (_super) {
    __extends(AddModelCommand, _super);
    /**
     * @param id The RFC4122 v4 compliant ID of this command.
     * @param requestId The RFC4122 v4 compliant ID of the request that created this command.
     * @param sessionId The RFC4122 v4 compliant ID of the session that created this command.
     * @param elementId The RFC4122 v4 compliant ID of the new model.
     * @param elementType The type of the new model.
     * @param elementProperties The properties of the new model.
     * @param parentId The optional ID of the parent element
     */
    function AddModelCommand(id, requestId, sessionId, elementId, elementType, elementProperties, parentId) {
        _super.call(this, id, requestId, sessionId, CommandType_1.CommandType.AddModel, elementId, elementType, elementProperties);
        this.ParentId = parentId;
    }
    return AddModelCommand;
}(AddCommand_1.AddCommand));
exports.AddModelCommand = AddModelCommand;
//# sourceMappingURL=AddModelCommand.js.map