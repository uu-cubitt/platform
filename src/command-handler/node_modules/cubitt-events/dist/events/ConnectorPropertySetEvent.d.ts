import * as Common from "cubitt-common";
import { PropertySetEvent } from "./PropertySetEvent";
export declare class ConnectorPropertySetEvent extends PropertySetEvent {
    constructor(sourceId: Common.Guid, version: number, timestamp: number, elementId: Common.Guid, propertyName: string, propertyValue: any);
}
