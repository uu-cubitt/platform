"use strict";
var __extends = (this && this.__extends) || function (d, b) {
    for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p];
    function __() { this.constructor = d; }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
};
var ElementType_1 = require("./ElementType");
var AbstractElement_1 = require("./AbstractElement");
/**
 * Element representing a Model
 */
var ModelElement = (function (_super) {
    __extends(ModelElement, _super);
    function ModelElement() {
        _super.apply(this, arguments);
    }
    /**
     * @inheritdoc
     */
    ModelElement.prototype.getType = function () {
        return ElementType_1.ElementType.Model;
    };
    /**
     * @inheritdoc
     */
    ModelElement.prototype.delete = function (graph) {
        /*
         * Then remove all nodes, which will remove their connectors
         * which will remove the edges
         * (connectors are not linked to the model directly)
         */
        var nodeIds = this.getChildNodeNeighbours();
        for (var _i = 0, nodeIds_1 = nodeIds; _i < nodeIds_1.length; _i++) {
            var nodeId = nodeIds_1[_i];
            var node = graph.getElement(nodeId);
            node.delete(graph);
        }
        // Remove model from any parent Nodes (if any)
        var parentNodes = this.getParentNodeNeighbours();
        for (var _a = 0, parentNodes_1 = parentNodes; _a < parentNodes_1.length; _a++) {
            var parentNodeId = parentNodes_1[_a];
            var parentNode = graph.getElement(parentNodeId);
            parentNode.unlinkChildModelNeighbour(this.id);
        }
        // Remove model from any parent Edges (if any)
        var parentEdges = this.getParentEdgeNeighbours();
        for (var _b = 0, parentEdges_1 = parentEdges; _b < parentEdges_1.length; _b++) {
            var parentEdgeId = parentEdges_1[_b];
            var parentEdge = graph.getElement(parentEdgeId);
            parentEdge.unlinkChildModelNeighbour(this.id);
        }
        this.remove(graph);
    };
    return ModelElement;
}(AbstractElement_1.AbstractElement));
exports.ModelElement = ModelElement;
//# sourceMappingURL=ModelElement.js.map