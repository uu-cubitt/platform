import * as Common from "cubitt-common";
import { EventType } from "./../EventType";
export declare abstract class Event {
    sourceId: Common.Guid;
    version: number;
    type: EventType;
    timestamp: number;
    constructor(sourceId: Common.Guid, version: number, type: EventType, timestamp: number);
    toJson(): string;
}
