import * as Common from "cubitt-common";
import { AddedEvent } from "./AddedEvent";
export declare class ModelAddedEvent extends AddedEvent {
    parentId: Common.Guid;
    constructor(sourceId: Common.Guid, version: number, timestamp: number, elementId: Common.Guid, elementType: string, elementProperties: Common.Dictionary<any>, parentId?: Common.Guid);
}
