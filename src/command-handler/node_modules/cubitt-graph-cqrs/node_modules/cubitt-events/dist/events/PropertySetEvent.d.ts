import * as Common from "cubitt-common";
import { Event } from "./Event";
import { EventType } from "./../EventType";
export declare abstract class PropertySetEvent extends Event {
    elementId: Common.Guid;
    propertyName: string;
    propertyValue: any;
    constructor(sourceId: Common.Guid, version: number, type: EventType, timestamp: number, elementId: Common.Guid, propertyName: string, propertyValue: any);
}
